import { test, expect } from '../../fixtures/app.fixture';
import { setMonacoValue, getMonacoValue } from '../../helpers/editor';

test('opens file via native dialog', async ({ app }) => {
  // TODO: implement when tauri-driver is configured
  expect(true).toBe(true);
});

test('saves file to disk', async ({ app }) => {
  // TODO: implement when tauri-driver is configured
  expect(true).toBe(true);
});

test('save as creates new file', async ({ app }) => {
  // TODO: implement when tauri-driver is configured
  expect(true).toBe(true);
});
